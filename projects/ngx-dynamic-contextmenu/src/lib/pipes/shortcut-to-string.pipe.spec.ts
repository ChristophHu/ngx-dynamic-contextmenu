import { ShortcutToStringPipe } from './shortcut-to-string.pipe';

describe('ShortcutToStringPipe', () => {
  it('create an instance', () => {
    const pipe = new ShortcutToStringPipe();
    expect(pipe).toBeTruthy();
  });
});
